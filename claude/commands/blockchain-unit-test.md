When this command is invoked, you MUST delegate the task to the `blockchain-unit-test` agent using the Agent tool. Do NOT attempt to perform test operations yourself — always use the Agent tool with the blockchain-unit-test agent.

Pass the user's arguments (if any) as the task prompt to the agent. If no arguments are given, instruct the agent to analyze the current working directory and suggest relevant tests to run or write.
